$contract { parameter string;
storage (pair (map string nat) (option bool)) ;


code { UNPAIR;
       DIP { CAR; DUP };
       MEM; SOME; SWAP; PAIR; NIL operation; PAIR;}
 } ;
$param "bar" ;
$storage (Pair {} None) ;
$ExpectedOutput (Pair {} (Some False)) ;

