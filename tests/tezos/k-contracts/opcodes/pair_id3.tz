$contract { parameter (pair bool bool);
storage (option (pair bool bool));
code {CAR; SOME; NIL operation; PAIR}
 } ;
$param (Pair False False) ;
$storage None ;
$ExpectedOutput (Some (Pair False False)) ;

