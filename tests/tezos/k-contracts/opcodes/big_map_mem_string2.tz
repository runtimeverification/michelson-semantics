$contract { parameter string;
storage (pair (big_map string nat) (option bool)) ;


code { UNPAIR;
       DIP { CAR; DUP };
       MEM; SOME; SWAP; PAIR; NIL operation; PAIR;}
 } ;
$param "foo" ;
$storage (Pair { Elt "foo" 0 } None) ;
$ExpectedOutput (Pair 0 (Some True)) ;

