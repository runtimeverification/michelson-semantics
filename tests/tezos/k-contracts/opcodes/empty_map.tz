$contract { storage (map string string);
parameter unit;
code {DROP;
      EMPTY_MAP string string;
      PUSH string "world"; SOME; PUSH string "hello"; UPDATE;
      NIL operation; PAIR};
 } ;
$param {} ;
$storage Unit ;
$ExpectedOutput { Elt "hello" "world" } ;

