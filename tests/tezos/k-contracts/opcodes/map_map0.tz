$contract { parameter nat;
storage (map string nat);


code { UNPAIR; SWAP;
       MAP { CDR; DIP {DUP}; ADD; };
       DIP { DROP; };
       NIL operation; PAIR; }
 } ;
$param 10 ;
$storage {} ;
$ExpectedOutput {} ;

