$contract { parameter (or int nat);
storage (option int);
code { CAR;
       IF_LEFT
         {
           NOT;
         }
         {
           NOT;
         } ;
       SOME; NIL operation ; PAIR
     }
 } ;
$param (Right 7) ;
$storage None ;
$ExpectedOutput (Some -8) ;

