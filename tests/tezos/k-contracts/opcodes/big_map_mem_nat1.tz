$contract { parameter nat;
storage (pair (big_map nat nat) (option bool)) ;


code { UNPAIR;
       DIP { CAR; DUP };
       MEM; SOME; SWAP; PAIR; NIL operation; PAIR;}
 } ;
$param 1 ;
$storage (Pair {} None) ;
$ExpectedOutput (Pair 0 (Some False)) ;

