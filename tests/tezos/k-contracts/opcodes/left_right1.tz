$contract { parameter (or bool string);
storage (or string bool);
code {CAR; IF_LEFT {RIGHT string} {LEFT bool}; NIL operation; PAIR};
 } ;
$param (Right "a") ;
$storage (Left "X") ;
$ExpectedOutput (Left "a") ;

