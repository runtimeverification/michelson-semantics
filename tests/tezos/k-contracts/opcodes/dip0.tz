$contract { parameter (pair nat nat);
storage (pair nat nat);
code{
      CAR; UNPAIR;
      DUP; DIP { ADD };
      PAIR;
      NIL operation;
      PAIR};
 } ;
$param (Pair 15 9) ;
$storage (Pair 0 0) ;
$ExpectedOutput (Pair 15 24) ;

