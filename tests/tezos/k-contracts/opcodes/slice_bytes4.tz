$contract { parameter (pair nat nat);
storage (option bytes);
code { UNPAIR; SWAP;
       IF_SOME {SWAP; UNPAIR; SLICE;} {DROP; NONE bytes;};
       NIL operation; PAIR}
 } ;
$param Pair 1 2 ;
$storage Some 0xaabbcc ;
$ExpectedOutput (Some 0xbbcc) ;

