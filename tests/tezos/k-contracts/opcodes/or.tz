$contract { parameter (pair bool bool);
storage (option bool);
code {CAR; DUP; CAR; SWAP; CDR; OR; SOME; NIL operation; PAIR};
 } ;
$param None ;
$storage (Pair True True) ;
$ExpectedOutput (Some True) ;

