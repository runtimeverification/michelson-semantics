$contract { parameter (pair nat nat);
storage (pair nat nat);
code{
      CAR; UNPAIR;
      DUP; DIP { ADD };
      PAIR;
      NIL operation;
      PAIR};
 } ;
$param (Pair 1 1) ;
$storage (Pair 0 0) ;
$ExpectedOutput (Pair 1 2) ;

