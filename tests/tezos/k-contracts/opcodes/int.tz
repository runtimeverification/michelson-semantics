$contract { parameter nat;
storage (option int);
# this contract takes a natural number as parameter, converts it to an
# integer and stores it.
code { CAR; INT; SOME; NIL operation; PAIR };
 } ;
$param None ;
$storage 9999 ;
$ExpectedOutput (Some 9999) ;

