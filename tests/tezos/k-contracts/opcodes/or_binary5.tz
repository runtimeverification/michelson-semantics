$contract { parameter (pair nat nat);
storage (option nat);


code { CAR;
       UNPAIR;
       OR;
       SOME; NIL operation; PAIR
     }
 } ;
$param (Pair 7 7) ;
$storage None ;
$ExpectedOutput (Some 7) ;

