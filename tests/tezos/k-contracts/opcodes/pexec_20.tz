$contract { parameter int;
storage (list int);
code {
      UNPAIR @p @s ; 
      LAMBDA (pair int (pair int int)) int
             { UNPAIR ; DIP { UNPAIR } ; ADD ; MUL }; 
      SWAP ; APPLY ; 
      PUSH int 3 ; APPLY ; 
      SWAP ; MAP { DIP { DUP } ; EXEC } ; 
      DIP { DROP } ; 
      NIL operation; PAIR };
 } ;
$param 4 ;
$storage { 0 ; 1 ; 2 ; 3} ;
$ExpectedOutput { 0 ; 7 ; 14 ; 21 } ;

