$contract { parameter string;
storage (pair (string %s) (nat %n));
code { DUP; CDR; DIP{CAR}; SET_CAR %s; NIL operation; PAIR };
 } ;
$param "" ;
$storage (Pair "hello" 0) ;
$ExpectedOutput (Pair "" 0) ;

