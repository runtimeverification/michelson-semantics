$contract { parameter nat;
storage nat;
code {
      LAMBDA (pair nat nat) nat
             {UNPAIR ; ADD};
      SWAP; UNPAIR ; DIP { APPLY } ; EXEC ; NIL operation; PAIR};
 } ;
$param 38 ;
$storage 14 ;
$ExpectedOutput 52 ;

